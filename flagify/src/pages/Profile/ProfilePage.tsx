import type { FC } from "react";
import {
    Header,
} from "./styles";

export const ProfilePage: FC = () =>  {

  return (
     <div>
      <Header>Profile</Header>
    </div>
  );
};

export default ProfilePage;